@using EntityFrameworkCoreDemo.Helpers
@model EntityFrameworkCoreDemo.Models.ViewModel.CompCvVM

@{
    ViewBag.Title = "Details";
    var countries = (ViewBag.CountryId as IEnumerable<SelectListItem>).ToDictionary(k=>k.Value,v=>v.Text);
    var counties = (ViewBag.CountyId as IEnumerable<SelectListItem>).ToDictionary(k=>k.Value,v=>v.Text);
}

<h2>Details</h2>

<div>
    <h4>CompCvVM</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.CvId)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CvId)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.FirstName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.FirstName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.LastName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.LastName)
        </dd>

        <dt>
            Country
        </dt>

        <dd>
            @(countries.GetValue(Model.CountryId.ToString()))
        </dd>

        <dt>
            County
        </dt>

        <dd>
            @(counties.GetValue(Model.CountyId.ToString()))
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    @Html.ActionLink("Back to List", "Index")
</p>
